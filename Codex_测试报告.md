# 中级经济师备考 App Codex 测试报告

**出具来源：** Codex  
**测试日期：** 2026-08-01  
**测试对象：** `备考app/docs` 静态 Web/PWA 应用及其题库  
**测试结论：** 不建议在修复 P0 问题前作为正式模拟考试或正式题库使用。

## 1. 测试范围与方法

| 范围 | 方法 | 结果 |
|---|---|---|
| 应用脚本与构建脚本 | JavaScript 语法检查、Python 编译检查 | 通过 |
| 静态资源 | 本地 HTTP 服务逐项请求首页、脚本、题库、图标及吉祥物资源 | 通过 |
| 页面功能 | 首页、刷题、错题本、模拟考、章节讲解及 56 个章节路由渲染 | 71 个路由通过 |
| 答题核心逻辑 | 单选、多选、部分分、模拟考抽题、错题间隔复习 | 通过 |
| 题库结构 | 2,206 道题的 ID、选项、答案、题型、科目、章节映射及重复检查 | 发现题库质量问题 |

### 覆盖的功能

1. 首页学习统计与科目入口。
2. 按科目、来源、章节的顺序/乱序刷题，以及“只练未做题”。
3. 单选、多选、即时判分与解析展示。
4. 错题本、错题统计、0→1→3→7 天间隔重复、四次答对毕业。
5. 经济基础与人力资源两种模拟考试、答题卡、倒计时、自动评分、部分分和历史成绩。
6. 56 个章节讲解、章节练习和考纲变动提醒。
7. 本地进度备份/恢复及 Service Worker 离线缓存。

## 2. 通过项

### 2.1 题库结构与抽题

- 题库共 **2,206** 道，ID 全部唯一：基础 1,391 道，人力 815 道。
- 每道题均具备题干、选项、答案、题型、科目和章节映射；答案均存在于当前选项中。
- 当前题库满足模拟考抽题规格：
  - 基础：70 单选 + 35 多选，共 105 题；
  - 人力：60 单选 + 40 多选，共 100 题。
- 多选题满分、少选部分分、错选零分的实现与当前代码规则一致。

**出处：**

- [题库数据](docs/bank.json)
- [题库统计](data/bank_stats.json)
- [模拟考试规格与评分](docs/app.js#L7-L10)、[抽题逻辑](docs/app.js#L551-L558)、[评分逻辑](docs/app.js#L625-L653)

### 2.2 路由与核心学习逻辑

- 首页、刷题、错题本、模拟考、章节页以及全部 56 个章节讲解页面均可完成渲染。
- 间隔复习逻辑：答错回到盒子 0；连续答对四次后移出错题本，逻辑测试通过。

**出处：**

- [刷题逻辑](docs/app.js#L204-L282)
- [作答及错题记录](docs/app.js#L315-L363)
- [间隔复习逻辑](docs/app.js#L365-L389)
- [章节数据](docs/chapters.json)、[讲解数据](docs/lecture.json)

## 3. 缺陷清单

## P0：模拟考在“答题卡”页面不再自动倒计时/交卷

**现象：** 进入答题卡后，页面没有 `#timer` 元素；已有倒计时回调再次执行时会直接返回，不再安排下一次检查。因此考生可以停留在答题卡直到考试结束，系统不会自动交卷。

**影响：** 模拟考时长失真，成绩不具备真实考试参考价值。

**证据出处：**

- [模拟考题目页创建计时器并启动计时](docs/app.js#L560-L579)
- [答题卡页面未创建计时器，也未继续计时](docs/app.js#L600-L609)
- [计时器在找不到 `#timer` 时直接退出](docs/app.js#L610-L623)

**修复建议：** 将倒计时从页面元素解耦，考试开始后持续以 `examState.endAt` 驱动；答题卡也应显示并启动同一个倒计时。超时后无论当前路由为何页面，都统一调用 `submitExam(true)`。

## P0：题库存在串题、题干截断与明确错误答案

### 已确认答案与题干/解析矛盾的题目

| 题目 ID | 当前答案 | 应为/问题 | 证据 |
|---|---:|---|---|
| `272c65278a57` | D | 应为 C“非排他性”；解析第一句已明确说明 | [题库](docs/bank.json) |
| `cca22400a47c` | B | 应为 D“借款”；解析明确写“借款是世界银行的主要资金来源” | [题库](docs/bank.json) |
| `6787f21b0b80` | AC | 应为 D“240”；解析给出中位数计算结果 240 | [题库](docs/bank.json) |
| `8d57a3986bee` | C | 应为 A“熊猫债”；解析明确说明境内人民币债为熊猫债 | [题库](docs/bank.json) |
| `7b3c4a7e9833` | A | 应为 D“偿债能力”；解析明确说明其为财务目标的稳健保证 | [题库](docs/bank.json) |
| `8b84fadeeca0` | C | 题干混入讲义后才出现实际题目；实际题目“负债项目排列”解析答案为 A | [题库](docs/bank.json) |
| `a20ba5981019` | B | 题干混入前一题文本；实际题目“商业银行资产业务规模”解析答案为 D“超额准备金” | [题库](docs/bank.json) |

### 已确认解析串入后续题目的记录

以下 8 条记录的解析含有后续的 `【单选】` 题目块，说明源文档切题失败；其中部分还伴随题干截断或答案错位：

- `7042337bc967`
- `272c65278a57`
- `84729ea7d1c3`
- `cca22400a47c`
- `6787f21b0b80`
- `8d57a3986bee`
- `7b3c4a7e9833`
- `a20ba5981019`

**来源出处：** [题库](docs/bank.json)，对应源文件位于 `data/questions_raw` 与 `资料库_文本提取/基础/真题`。

**修复建议：** 先将上述记录从正式题池隔离；重新从原始真题按题号、选项、答案、解析四段式解析，再人工复核答案。不要仅依赖前端 `sanitizeBank()`，因为这些题在结构上仍可通过前端过滤。

## P1：离线首次安装缓存不完整

**现象：** 应用启动必须读取 `bank.json`、`chapters.json`、`lecture.json` 和 `changes.json`，但安装阶段只预缓存了 `bank.json`。首次在网络不可用时打开，章节/讲解数据可能加载失败。

**出处：**

- [运行时数据加载](docs/app.js#L103-L113)
- [预缓存清单](docs/sw.js#L1-L9)

**修复建议：** 把 `chapters.json`、`lecture.json` 和 `changes.json` 加入 `ASSETS`，并升级缓存版本号。

## P1：刷题页存在参数注入风险

**现象：** `subject`、`src`、`chap` 来自 URL Hash 或本地恢复进度，随后直接拼入内联 `onclick` 代码。恶意分享链接或恶意备份码可造成脚本注入。

**出处：** [参数读取](docs/app.js#L205-L210)、[直接插值到 onclick](docs/app.js#L215-L230)。

**修复建议：** 对参数采用白名单（科目、来源、章节 ID），不再使用内联事件字符串；改用 `addEventListener` 和 `data-*` 属性绑定事件。

## P2：题库重复与章节标记质量风险

### 完全重复题

1. 人力资本投资错误题：`7bdaaf584d59`（2024 真题）与 `e8749f6ebb17`（2025 模拟题）。
2. 同位法新旧规定适用题：`489478910a51`（2025 模拟题）与 `941f9a0b7586`（习题）。

**出处：** [题库](docs/bank.json)。

### 低置信度章节标签

- 513 道在用题目的 `chapter_conf` 为 `ai`。
- 这不会必然导致答案错误，但会影响“按章节练习”和“薄弱章节”统计的可信度。

**出处：** [题库](docs/bank.json)、[低置信度清单](data/low_conf_ids.json)。

## 4. 其他观察

- “只练未做题”按钮在没有未做题时仍可点击，随后才弹出“没有符合条件的题目”；建议直接禁用按钮以减少无效交互。
- 页面宣传的是 2026 年考试，但章节变动数据及大部分资料仍标注 2025；上线前应明确 2026 年大纲与教材是否已完成复核。

**出处：** [只练未做题按钮](docs/app.js#L224-L230)、[章节变动数据](docs/changes.json)。

## 5. 测试限制

本次环境没有可用的浏览器自动化实例，因此未覆盖真实手机浏览器视觉布局、手势、PWA 安装弹窗和离线切换的人工验收。已完成的页面测试为脚本级路由渲染和本地 HTTP 资源加载测试。

## 6. 建议修复顺序

1. 修复模拟考答题卡计时器（P0）。
2. 隔离并重建 8 条串题记录，优先修正上表 7 条明确错误题（P0）。
3. 补齐 Service Worker 预缓存（P1）。
4. 对 URL 与备份恢复数据做白名单校验，移除内联事件拼接（P1）。
5. 去重并人工抽审 513 道 AI 章节标签题（P2）。
